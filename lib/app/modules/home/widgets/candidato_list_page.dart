import 'package:flutter/material.dart';

class CandidatoListPage extends StatelessWidget {
  const CandidatoListPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Card(
      elevation: 5,
      child: Column(children: []),
    );
  }
}
